name: Facebook AI Bot Auto-Setup
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Unzip and Fix Dependencies
        run: |
          unzip bot.zip
          cd TatsuYTB || cd .
          
          # تثبيت المكتبات الناقصة التي ظهرت في الخطأ
          npm install bluebird axios uuid@8.3.2
          
          # تثبيت بقية المكتبات الموجودة في المشروع
          npm install --production || true
          
          # نقل الكوكيز
          cp ../appstate.json .

      - name: Configure and Inject AI
        run: |
          cd TatsuYTB || cd .
          sed -i 's/"PREFIX": ""/"PREFIX": "."/g' config.json
          sed -i 's/"ADMINBOT": \[ "" \]/"ADMINBOT": [ "100000917675367" ]/g' config.json
          
          # حقن منطق الذكاء الاصطناعي
          cat << 'AI' > ai_logic.js
          const axios = require('axios');
          async function getReply(text) {
            try {
              const r = await axios.post('https://openrouter.ai/api/v1/chat/completions', {
                model: 'openai/gpt-3.5-turbo',
                messages: [{role: 'user', content: text}]
              }, {
                headers: { Authorization: 'Bearer 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' }
              });
              return r.data.choices[0].message.content;
            } catch (e) { return "🤖 خطأ في الاتصال بالذكاء الاصطناعي"; }
          }
          module.exports = { getReply };
          AI

      - name: Start Facebook Bot
        run: |
          cd TatsuYTB || cd .
          # تشغيل البوت وإبقاؤه يعمل لمدة ساعة
          node index.js & sleep 3600
