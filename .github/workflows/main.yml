name: Facebook AI Bot Auto-Setup
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16  # نستخدم إصدار 16 لزيادة التوافق مع البوتات القديمة

      - name: Unzip and Fix Dependencies
        run: |
          unzip bot.zip
          # الدخول للمجلد الذي يحتوي على الكود
          cd TatsuYTB || cd .
          
          # تثبيت المكتبات الأساسية
          npm install
          
          # حل مشكلة ERR_REQUIRE_ESM بإجبار السيرفر على نسخة قديمة متوافقة
          npm install uuid@8.3.2 axios
          
          # نقل ملف الكوكيز لمكانه الصحيح
          cp ../appstate.json .

      - name: Inject AI Logic
        run: |
          # حقن الذكاء الاصطناعي داخل مجلد البوت
          cd TatsuYTB || cd .
          cat << 'AI' > ai_logic.js
          const axios = require('axios');
          async function getReply(text) {
            try {
              const r = await axios.post('https://openrouter.ai/api/v1/chat/completions', {
                model: 'openai/gpt-3.5-turbo',
                messages: [{role: 'user', content: text}]
              }, {
                headers: { Authorization: 'Bearer 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' }
              });
              return r.data.choices[0].message.content;
            } catch (e) { return "🤖 خطأ في الاتصال بالذكاء الاصطناعي"; }
          }
          module.exports = { getReply };
          AI

      - name: Start Facebook Bot
        run: |
          cd TatsuYTB || cd .
          # تشغيل البوت مباشرة
          node index.js
